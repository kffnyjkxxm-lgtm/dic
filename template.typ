#set page(
  paper: "a4",
  margin: (x: 1cm, y: 1.5cm),
  columns: 4,
  footer: context align(center)[
    #set text(8pt, fill: gray)
    #counter(page).display("1")
  ]
)

#set text(size: 8pt)

$body$